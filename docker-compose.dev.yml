version: '2.1'

services:
  rabbitmq:
    ports:
      - 5672:5672

  config:
    build: config
    ports:
      - 8888:8888

  auth-service:
    build: auth-service

  registry:
    build: registry

  gateway:
    build: gateway

  bookstore:
    build: bookstore

  monitoring:
    build: monitoring